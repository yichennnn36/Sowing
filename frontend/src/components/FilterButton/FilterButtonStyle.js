import styled from 'styled-components';

export const FilterWrapper = styled.div`


  & button {
    transform: scale(1.2);
    margin: 0 10px;
    border: transparent;

    &:hover {
      transform: scale(1.4);
    }
  }
`;
