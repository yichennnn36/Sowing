import React from 'react';
import { LoadingWrapper, LoadingIcon } from './LoadingStyle';

const Loading = () => {
  return (
    <LoadingWrapper>
      <LoadingIcon />
    </LoadingWrapper>
  )
}

export default Loading;